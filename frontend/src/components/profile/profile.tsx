import React, { FunctionComponent, ReactElement } from 'react';

const Profile: FunctionComponent = ():ReactElement =>{
  return <div style={{ width:'201px', backgroundColor:'pink' }} />
};

export default Profile;
