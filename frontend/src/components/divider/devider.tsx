import { FunctionComponent, ReactElement } from 'react';
import classes from './divider.module.scss';

const Divider: FunctionComponent = (): ReactElement => (<div className={classes.divider}/>);

export default Divider;
